# render.yaml
services:
  - type: web
    name: n8n
    env: docker
    region: oregon
    plan: free
    autoDeploy: true
    healthCheckPath: /healthz

    # CORRECT: Use block style for image
    image:
      repository: docker.n8n.io/n8nio/n8n
      tag: latest

    dockerCommand: n8n start
